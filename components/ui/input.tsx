import React from 'react';
import { View, TextInput, type TextInputProps, type ViewProps } from 'react-native';

// Gluestack-compatible Input components using NativeWind
// Input is a styled container, InputField is the actual text input

export const Input = View;

export const InputField = TextInput;
