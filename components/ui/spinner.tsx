import { ActivityIndicator } from 'react-native';

// Gluestack-compatible Spinner component using NativeWind
// Spinner is a styled wrapper around React Native's ActivityIndicator
export const Spinner = ActivityIndicator;
