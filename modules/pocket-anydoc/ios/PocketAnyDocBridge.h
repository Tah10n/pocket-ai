#ifndef POCKET_ANYDOC_BRIDGE_H
#define POCKET_ANYDOC_BRIDGE_H

/* CocoaPods file patterns cannot traverse above the pod root. Keep this
 * wrapper declaration-free and resolve the single ABI header through the
 * local module header search path. */
#include <pocket_anydoc.h>

#endif
